export async function svgRaster(path,size,color,original=false){const response=await fetch('/assets/'+path);if(!response.ok)throw new Error('No se encontró el recurso local.');let svg=await response.text();if(!original)svg=svg.replaceAll('currentColor',color);const blob=new Blob([svg],{type:'image/svg+xml'}),url=URL.createObjectURL(blob);try{const img=new Image();img.src=url;await img.decode();const canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;canvas.getContext('2d').drawImage(img,0,0,size,size);return {width:size,height:size,rgba:[...canvas.getContext('2d').getImageData(0,0,size,size).data]};}finally{URL.revokeObjectURL(url);}}
