export class HistoryManager {undoStack=[];redoStack=[];push(project){this.undoStack.push(structuredClone(project));if(this.undoStack.length>80)this.undoStack.shift();this.redoStack=[];}undo(current){if(!this.undoStack.length)return current;this.redoStack.push(structuredClone(current));return this.undoStack.pop();}redo(current){if(!this.redoStack.length)return current;this.undoStack.push(structuredClone(current));return this.redoStack.pop();}}
