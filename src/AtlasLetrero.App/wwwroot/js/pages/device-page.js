import {header,matrixFields,el,$,safe,toast,api} from '../app.js';
import {matrixFromForm} from '../project/project-model.js';
import {select,field} from '../core/dialog.js';
header();const id=new URLSearchParams(location.search).get('id');let project=id?await api('projects/'+id):null;
const form=$('#matrix-form');form.append(matrixFields(project?.matrix),el('br'),el('div',{class:'form-grid'},select('Rotación','rotation',[[0,'0°'],[90,'90°'],[180,'180°'],[270,'270°']],project?.matrix.rotation||0),el('div',{},el('label',{},el('input',{type:'checkbox',name:'flipX',checked:project?.matrix.flipX||false}),'Reflejar horizontalmente'),el('label',{},el('input',{type:'checkbox',name:'flipY',checked:project?.matrix.flipY||false}),'Reflejar verticalmente'))),el('div',{class:'actions'},el('button',{type:'submit',class:'primary',text:'Guardar configuración',disabled:!project,title:project?'Guardar los ajustes de esta matriz':'Abre un proyecto para guardar su configuración'})));
if(id)$('#back').href='/editor.html?id='+id;
form.addEventListener('submit',safe(async event=>{event.preventDefault();project.matrix=matrixFromForm(new FormData(form));await api('projects/'+id,'PUT',project);toast('Configuración guardada.');}));
const ports=el('select',{'aria-label':'Puerto USB / Serial'},el('option',{value:'',text:'Sin puertos detectados'})),status=el('p',{text:'○ Sin dispositivo',role:'status'}),details=el('p',{class:'muted',text:'Firmware: — · Protocolo: — · Resolución reportada: —'}),connect=el('button',{text:'Conectar',disabled:true}),disconnect=el('button',{text:'Desconectar',disabled:true}),test=el('button',{text:'Probar matriz',disabled:true,title:'Requiere firmware AtlasLED y transporte de pruebas completo'});
const detect=el('button',{text:'Detectar USB / Serial',onclick:safe(async()=>{detect.disabled=true;status.textContent='Detectando puertos…';try{const result=await api('devices/serial');ports.replaceChildren(...(result.ports.length?result.ports.map(p=>el('option',{value:p,text:p+' · sin identificar'})):[el('option',{value:'',text:'Sin puertos detectados'})]));connect.disabled=!result.ports.length;status.textContent=result.ports.length?'Selecciona un puerto para comprobar el handshake.':'○ Sin dispositivo';}finally{detect.disabled=false;}})});
connect.addEventListener('click',safe(async()=>{connect.disabled=true;status.textContent='Comprobando handshake…';try{await api('devices/connect','POST',{port:ports.value});await refresh();}catch(error){status.textContent='No se identificó un dispositivo AtlasLED.';throw error;}finally{connect.disabled=!ports.value;}}));
disconnect.addEventListener('click',safe(async()=>{await api('devices/disconnect','POST');await refresh();}));
async function refresh(){const result=await api('devices/status');status.textContent=result.connected?'● ESP32 conectado':'○ Sin dispositivo';disconnect.disabled=!result.connected;details.textContent=result.connected?'Firmware: '+result.capabilities.firmware+' · Protocolo: '+result.capabilities.protocol+' · '+result.capabilities.width+'×'+result.capabilities.height:'Firmware: — · Protocolo: — · Resolución reportada: —';}
$('#device').append(detect,el('br'),el('br'),el('label',{},'Puerto',ports),status,details,el('div',{class:'row'},connect,disconnect,test));await safe(refresh)();
