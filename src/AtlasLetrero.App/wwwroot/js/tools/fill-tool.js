export function floodPoints(buffer,start){const {width:w,height:h,data}=buffer;if(start.x<0||start.y<0||start.x>=w||start.y>=h)return [];const target=data.slice((start.y*w+start.x)*4,(start.y*w+start.x)*4+3),queue=[start],seen=new Set(),points=[];while(queue.length){const p=queue.pop(),i=p.y*w+p.x;if(p.x<0||p.x>=w||p.y<0||p.y>=h||seen.has(i))continue;seen.add(i);if(!target.every((v,c)=>data[i*4+c]===v))continue;points.push(p);queue.push({x:p.x+1,y:p.y},{x:p.x-1,y:p.y},{x:p.x,y:p.y+1},{x:p.x,y:p.y-1});}return points;}
